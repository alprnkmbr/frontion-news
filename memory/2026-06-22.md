# 2026-06-22 Memory

## LinkedIn Pipeline — Major Updates

### Image Cards for All Posts
- Every LinkedIn post (BLUF, Sections, Bottom Line) now has both text AND an image card
- `linkedin_cards.py` (NEW): Generates card PNGs for all post types with unified design:
  - White bg, Georgia serif font, red accent bars top/bottom
  - "FRONTION NEWS / Strategic Brief / date" header
  - Page numbers (1/N, 2/N... N/N) in top-right
  - Justified text, large bold headlines (60px for BLUF/Bottom Line, 36px for sections)
  - Cards saved to `linkedin-cards/` dir, pushed to GitHub Pages
- `linkedin_post.py` (UPDATED): Now generates card + pushes to GitHub + sends `image_url` with every post
- Make.com Router unchanged — all posts go through Route 1 (Image Post) since all now have `image_url`

### Card Design Decisions
- BLUF: Large bold title (60px), justified subhead (34px), ◆ removed from image (stays in text)
- Section: Heading (36px), justified body + "Why it matters:" with red accent line, ► in text only
- Bottom Line: "The Bottom Line" heading (60px), justified text (34px), ■ in text only, not in image
- Page numbers format: "1/8", "2/8"... last page is "8/8"

### Hashtags Added
- `linkedin_post.py` now appends content-based hashtags to every post
- Base hashtags: `#Geopolitics #Strategy` (always included)
- Topic-specific hashtags selected from content analysis (keyword mapping)
- Max 5 hashtags per post (3-4 for Bottom Line)
- Keywords map to tags like: MiddleEast, EnergySecurity, Diplomacy, TradeWar, etc.

### Cron Jobs — Dynamic Scheduling
- Old fixed section/bottomline cron jobs deleted
- From now on: every morning when brief is published, I'll create cron jobs based on section count
- No more hardcoded `section_num = current_hour - 7` formula
- Section 6 was missed today (June 22) due to old formula bug — sent manually
- Bottom Line also sent manually today (with user's implicit approval, but acknowledged rule: always ask first)

### Domain: frontion.news
- Purchased on Hostinger (~455 TL/year)
- DNS configured: 4x A records (GitHub Pages IPs) + CNAME www → alprnkmbr.github.io
- CNAME file added to repo
- Custom domain set in GitHub Pages settings
- HTTPS cert requested (Let's Encrypt via GitHub)
- DNS propagated on Google DNS (8.8.8.8) but ISP DNS may take a few hours
- LinkedIn Company Page website updated to www.frontion.news (LinkedIn doesn't accept bare domain)

### YouTube Bot Update
- `bot.py` updated: YouTube video descriptions now include `🌐 https://frontion.news` link

### Make.com Filter
- Added filter before Router: `{{1.body}}` Is not empty
- Prevents empty `{}` webhook payloads from triggering errors

### Rules / Lessons
- NEVER send manual LinkedIn posts without explicit user approval
- Section cron formula was buggy — now dynamic per-day scheduling
- Make.com sometimes sends empty `{}` — filter added to handle this
- BLUF card: ◆ removed from image, stays in text
- Bottom Line text: ■ stays (user specifically reminded)
- LinkedIn doesn't support Markdown — only plain text + unicode symbols

### Files Changed/Created
- `/Users/claudius/clawd/frontion-site/linkedin_cards.py` (NEW) — card image generator
- `/Users/claudius/clawd/frontion-site/linkedin_post.py` (UPDATED) — hashtags + image generation
- `/Users/claudius/clawd/frontion-site/CNAME` (NEW) — contains `frontion.news`
- `/Users/claudius/frontion-bot/bot.py` (UPDATED) — YouTube description now includes frontion.news link

### Pending
- DNS may still not resolve on some ISPs — should be fine by tomorrow
- Need to commit linkedin_cards.py and linkedin_post.py changes
- Old `linkedin_card.py` can be cleaned up (replaced by `linkedin_cards.py`)
- Old BLUF-only card (`linkedin-card.png`) is now replaced by per-type cards in `linkedin-cards/`
- YouTube bot needs `frontion-bot` repo commit for the description change